---
import { Image } from 'astro:assets';
import flags from '../assets/flags-pe-kr.png';
import bg from '../assets/tv-section.png';
import googleTv from '../assets/tech/google-tv.png';
import dolby from '../assets/tech/dolby-atmos.png';
import cast from '../assets/tech/google-cast.png';
import heyGoogle from '../assets/tech/hey-google.png';
import hdmi from '../assets/tech/hdmi.png';
import hdr10 from '../assets/tech/hdr10.png';

const logos = [
  { img: dolby, alt: 'Dolby Vision · Atmos' },
  { img: cast, alt: 'Google Cast' },
  { img: heyGoogle, alt: 'Hey Google' },
  { img: hdmi, alt: 'HDMI 2.1 Gaming' },
  { img: hdr10, alt: 'HDR10+' },
];
---

<section class="tech" id="tv">
  <p class="warranty">
    <Image src={flags} alt="Perú · Corea" width={278} height={21} densities={[1, 2]} />
    <span>Garantía especial <strong>DAEWOO OFICIAL</strong></span>
  </p>

  <div class="tv">
    <Image
      class="tv-bg"
      src={bg}
      alt=""
      aria-hidden="true"
      widths={[768, 1280]}
      sizes="100vw"
    />
    <div class="tv-inner" data-reveal>
      <Image class="gtv" src={googleTv} alt="Google TV" height={46} densities={[1, 2, 3]} format="png" loading="lazy" />
      <ul class="logos">
        {logos.map((l) => (
          <li>
            <Image src={l.img} alt={l.alt} height={26} densities={[1, 2, 3]} format="png" loading="lazy" />
          </li>
        ))}
      </ul>
      <p>
        Descubre cómo los televisores con Google TV transforman tu forma de ver series, películas y
        más. Todo lo que amas, en un solo lugar.
      </p>
    </div>
  </div>
</section>

<style>
  .warranty {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
    padding: 0.95rem 1rem;
    background: var(--c-blue);
    color: var(--c-white);
    font-size: clamp(0.875rem, 0.82rem + 0.35vw, 1.0625rem);
    font-weight: 500;
    text-align: center;
  }
  .warranty :global(img) {
    height: 20px;
    width: auto;
  }

  .tv {
    position: relative;
    isolation: isolate;
    background: var(--c-black);
    color: var(--c-white);
    overflow: hidden;
  }
  .tv-bg {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    /* .tv's height is content-driven (logo + tech icons + copy), so it rarely
       matches tv-section.png's native 1280:240 ratio exactly. `cover` crops
       the sides whenever the section is proportionally taller than that
       (common from mobile up to ~1280px wide); `contain` never crops but
       letterboxes left/right whenever the section is proportionally WIDER
       than that (common above ~1280px). Neither is right on its own — the
       script below measures the actual box each time and picks whichever one
       doesn't crop/pad for real, so this is just the safe pre-JS fallback. */
    object-fit: contain;
    object-position: center;
    z-index: -1;
  }
  .tv::after {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(90deg, rgba(0, 0, 0, 0.92) 0%, rgba(0, 0, 0, 0.72) 42%, rgba(0, 0, 0, 0.35) 100%);
    z-index: -1;
  }

  .tv-inner {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 0.85rem;
    width: fit-content;
    max-width: min(92vw, 600px);
    margin-left: max(var(--pad-x), calc((100vw - var(--container)) / 2));
    padding-block: clamp(1.75rem, 4.5vw, 2.75rem);
    padding-right: var(--pad-x);
  }
  /* Google TV centred over the logo row below it */
  .gtv {
    align-self: center;
    height: clamp(32px, 3.6vw, 46px);
    width: auto;
  }
  .logos {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: center;
    gap: 0.7rem 1.4rem;
    list-style: none;
    padding: 0;
    margin: 0;
  }
  .logos :global(img) {
    height: clamp(20px, 2vw, 26px);
    width: auto;
  }
  .tv-inner p {
    max-width: 52ch;
    font-size: var(--fs-body);
    color: var(--c-grey-light);
  }

  @media (max-width: 640px) {
    .tv-inner {
      width: auto;
      max-width: 100%;
      align-items: center;
      text-align: center;
      margin-inline: auto;
      padding-inline: var(--pad-x);
      gap: 1rem;
    }
    .logos {
      gap: 0.6rem 1.1rem;
    }
  }
</style>

<script>
  // Pick cover vs. contain by actually comparing the section's live aspect
  // ratio to the image's — content-driven height means neither is safe on
  // its own at every width (see the CSS comment on .tv-bg).
  const tv = document.querySelector<HTMLElement>('#tv .tv');
  const bg = document.querySelector<HTMLImageElement>('#tv .tv-bg');
  if (tv && bg) {
    const update = () => {
      const w = tv.clientWidth;
      const h = tv.clientHeight;
      const imgW = bg.naturalWidth || 1280;
      const imgH = bg.naturalHeight || 240;
      if (!w || !h || !imgW || !imgH) return;
      bg.style.objectFit = w / h >= imgW / imgH ? 'cover' : 'contain';
    };
    if (bg.complete) update();
    else bg.addEventListener('load', update, { once: true });
    new ResizeObserver(update).observe(tv);
  }
</script>
